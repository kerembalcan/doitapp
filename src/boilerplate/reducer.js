/* @flow */

export type BoilerplateState = {
};

export const boilerplateReducerInitialState = {
};

export default function boilerplateReducer(state: BoilerplateState = boilerplateReducerInitialState, action: Object) {
  switch (action.type) {
    default:
      return state
  }
}